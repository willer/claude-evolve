#!/bin/bash
# Build and launch the Evolve Greenhouse Electron app (greenhouse/).
# Run from anywhere: ./run-greenhouse
# Delegates to package-and-run.sh, which packages the real .app and opens it
# (npm install/build happen inside its package.sh step).
set -e
exec "$(dirname "$0")/greenhouse/package-and-run.sh" "$@"
